import {Download,FileSpreadsheet,FileText} from "lucide-react";
import PageHeader from "../components/common/PageHeader";
import Button from "../components/ui/Button";

export default function ReportsPage(){const reports=[{name:"Sales report",description:"Revenue, orders and average order value."},{name:"Inventory report",description:"Stock on hand, low stock and out-of-stock items."},{name:"Customer report",description:"Customer growth, spend and order frequency."},{name:"Promotion report",description:"Coupon usage and promotion performance."}];return <><PageHeader title="Reports" subtitle="Generate operational and commercial reports for your team."/><div className="grid gap-5 md:grid-cols-2">{reports.map((report,index)=><div key={report.name} className="rounded-2xl border border-gray-100 bg-white p-5"><div className="flex items-start justify-between gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-primary-50 text-primary-500">{index%2===0?<FileText size={20}/>:<FileSpreadsheet size={20}/>}</div><Button size="sm" variant="outline"><Download size={15}/>Export</Button></div><h2 className="mt-4 font-black">{report.name}</h2><p className="mt-2 text-sm leading-6 text-gray-500">{report.description}</p><div className="mt-4 flex gap-2"><input type="date" className="h-10 min-w-0 flex-1 rounded-xl border border-gray-200 px-3 text-sm"/><input type="date" className="h-10 min-w-0 flex-1 rounded-xl border border-gray-200 px-3 text-sm"/></div></div>)}</div></>}
